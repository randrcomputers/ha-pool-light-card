Copy the images you want into **`config/www/pool_card/`** on Home Assistant (SSH, Samba, or File editor).

## Recommended for the Pool Light Card

| Copy this file | Example URL | Shown when |
| --- | --- | --- |
| **`pool_light_fixture.png`** | `/local/pool_card/pool_light_fixture.png` | Light **on** (default **Auto**) |
| **`light_control_box.png`** | `/local/pool_card/light_control_box.png` | Light **off** |

Set **Fixture vs control box** to **Auto** in the card editor (default).

## Your photos (optional)

| File | Use |
| --- | --- |
| **`ipool_light.png`** | Real product photo (white background). URL: `/local/pool_card/ipool_light.png` — tune **Lens glow** in the editor so color sits on the lens. |
| **`light_control_box.png`** | Physical controller reference (not used by the card UI; handy for docs or a picture-elements view). |

## Other bundled art

| File | Notes |
| --- | --- |
| **`pool_light_controller.png`** | AI controller panel (no branding), dark background |
| **`pool_light.png`** | Earlier AI fixture variant — optional |
